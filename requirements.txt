# ==============================================================================
# AutoPhotoSorter — requirements.txt
# ==============================================================================
# Встановлення всіх обов'язкових залежностей:
#   pip install -r requirements.txt
#
# Встановлення опціональних залежностей описано нижче.
# ==============================================================================

# ------------------------------------------------------------------------------
# ОБОВ'ЯЗКОВІ залежності
# ------------------------------------------------------------------------------

# Обробка зображень (OpenCV — визначення білого фону)
opencv-python>=4.8.0

# Робота з зображеннями (PIL/Pillow)
Pillow>=10.0.0

# Числові обчислення (транзитивна залежність OpenCV/Pillow)
numpy>=1.24.0

# Генерація Excel-звіту
openpyxl>=3.1.2

# HTTP запити (для Ollama API)
requests>=2.31.0

# ------------------------------------------------------------------------------
# ОПЦІОНАЛЬНІ залежності — OCR (виявлення тексту та водяних знаків)
# ------------------------------------------------------------------------------
# Потребує встановлення системного пакету Tesseract:
#   Windows : https://github.com/UB-Mannheim/tesseract/wiki
#   Linux   : sudo apt-get install tesseract-ocr
#   macOS   : brew install tesseract
#
# Після встановлення Tesseract виконайте:
#   pip install pytesseract

pytesseract>=0.3.10

# ------------------------------------------------------------------------------
# ОПЦІОНАЛЬНІ залежності — AI API (класифікація контенту)
# Розкоментуйте потрібний варіант або встановіть вручну.
# ------------------------------------------------------------------------------

# Google Gemini API (рекомендовано — швидко, доступно, модель gemini-1.5-flash)
# Отримати ключ: https://aistudio.google.com/app/apikey
google-generativeai>=0.5.0

# OpenAI Vision API (модель gpt-4o-mini)
# Отримати ключ: https://platform.openai.com/api-keys
openai>=1.0.0

# ------------------------------------------------------------------------------
# ОПЦІОНАЛЬНІ залежності — Ollama (локальні моделі, наприклад llava, bakllava)
# ------------------------------------------------------------------------------
# Для використання Ollama потрібно:
# 1. Встановити Ollama: https://ollama.ai/
# 2. Запустити Ollama сервер: ollama serve
# 3. Завантажити модель з підтримкою зображень: ollama pull llava
# Бібліотека requests вже включена у обов'язкові залежності вище.

# ------------------------------------------------------------------------------
# ОПЦІОНАЛЬНІ залежності — Локальна CLIP модель (~350 MB, без API)
# ------------------------------------------------------------------------------
# PyTorch (CPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# PyTorch (CUDA — для GPU прискорення):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# CLIP:
#   pip install git+https://github.com/openai/CLIP.git
#
# НЕ розкоментовуйте тут — встановлюйте вручну за інструкцією вище.
# torch>=2.0.0
# torchvision>=0.15.0
